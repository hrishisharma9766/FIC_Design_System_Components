/* EHR-Nav.css */

:root {
  /* Panel and Backgrounds */
  --ehr-nav-bg: var(--Colors-Navigation-EHR_TopNavigation-Bkg_Panel, #303030);
  --ehr-nav-selection: var(--Colors-Navigation-EHR_TopNavigation-Bkg_Menu_Selection, #4B4B4A);
  --ehr-nav-divider: var(--Colors-Navigation-EHR_TopNavigation-Border_Divider, #71706C);
  
  /* Icons and Text */
  --ehr-icon-default: var(--Colors-Navigation-EHR_TopNavigation-Icon_Default, white);
  --ehr-txt-label: var(--Colors-Navigation-EHR_TopNavigation-Txt_Label_Default, white);
  --ehr-txt-nav: var(--Colors-Navigation-Other-Txt_NavigaionLabels_Default_White, white);
  
  /* Status Colors */
  --ehr-count-active: var(--Colors-Navigation-Other-Bkg_ActiveCount, #E73D36);
}

/* Main Container */
.ehr-top-nav {
  width: 100%;
  background: var(--ehr-nav-bg);
  display: flex;
  flex-direction: column;
  padding: 2px;
  box-sizing: border-box;
  font-family: 'DM Sans', sans-serif;
}

/* Top Row (Menu + Profile) */
.ehr-nav-row-main {
  align-self: stretch;
  height: 48px;
  padding: 0 8px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* Menu Item Container */
.ehr-nav-menu-group {
  display: flex;
  align-items: center;
  gap: 12px;
}

/* Base Nav Item Style */
.ehr-nav-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px;
  border-radius: 13px;
  background: transparent;
  transition: background 0.2s ease;
}

/* Active/Selected State (Practice Hub) */
.ehr-nav-item[data-state="Selected"] {
  background: var(--ehr-nav-selection);
}

/* Nav Item Labels */
.ehr-nav-label {
  color: var(--ehr-txt-label);
  font-size: 14px;
  font-weight: 400; /* Dashboard, Inventory, etc. */
  line-height: 20.37px;
  word-wrap: break-word;
}

.ehr-nav-item[data-state="Selected"] .ehr-nav-label {
  font-weight: 800; /* Active state font weight */
}

/* Badge / Count Styles (99+) */
.ehr-count-badge {
  background: var(--ehr-count-active);
  border-radius: 13px;
  padding: 0 4px;
  min-width: 20px;
  height: 20px;
  display: flex;
  justify-content: center;
  align-items: center;
  color: white;
  font-size: 10px;
  font-weight: 800;
  line-height: 14.55px;
}

/* Utility Row (Search, Alerts, User) */
.ehr-nav-utility {
  display: flex;
  align-items: center;
  gap: 10px;
  padding-left: 12px;
  padding-right: 6px;
}

/* User Profile (Avatar AD) */
.ehr-nav-profile {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 40px;
  padding: 0 8px;
  border-left: 1px solid var(--ehr-nav-divider);
}

.ehr-avatar-circle {
  width: 32px;
  height: 32px;
  background: var(--ehr-nav-selection);
  border-radius: 50%;
  display: flex;
  justify-content: center;
  align-items: center;
  color: var(--ehr-txt-label);
  font-size: 14px;
  font-weight: 600;
  line-height: 21px;
}

/* Lower Row (Sub-Tabs / Practice Locations) */
.ehr-nav-sub-row {
  align-self: stretch;
  background: var(--ehr-nav-bg);
  border-top: 1px solid var(--ehr-nav-divider);
  padding: 0 8px;
  display: flex;
}

.ehr-tab-active {
  background: var(--ehr-nav-selection);
  border-top-left-radius: 13px;
  border-top-right-radius: 13px;
  border-bottom: 3px solid var(--ehr-txt-label);
  padding: 8px 16px;
  color: var(--ehr-txt-label);
  font-size: 14px;
  font-weight: 800;
  line-height: 20.37px;
}